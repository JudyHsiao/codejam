#include <cstdio>
#include <string>
#include <vector>
#include <iostream>

using namespace std;
const int maxn = 25;
vector<int> A[maxn];
int main() {
    int n;

    cin >> n;
    string s1, s2;
    for(int i = 0; i < n; i++)
        A[i].push_back(i);
    

}